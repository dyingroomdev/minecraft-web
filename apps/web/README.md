# AMZCraft Web

React + Vite + Tailwind + shadcn/ui front-end for the AMZCraft monorepo.

## Available scripts

```bash
npm install
npm run dev
npm run build
npm run preview
```

Tailwind and shadcn/ui are configured out of the box with a sample button component in `src/components/ui/button.tsx`.
