# AMZCraft Admin

Standalone administration dashboard.

```bash
cp .env.example .env
npm install
npm run dev
```

Development URL: `http://localhost:5174/admin`

The dashboard uses the API's `/admin` endpoints and stores its admin access
token independently from the public frontend.
